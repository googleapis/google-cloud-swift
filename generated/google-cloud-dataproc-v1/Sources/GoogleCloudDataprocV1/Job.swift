// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWkt

/// A Dataproc job resource.
public struct Job: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Optional. The fully qualified reference to the job, which can be used to
  /// obtain the equivalent REST path of the job resource. If this property
  /// is not specified when a job is created, the server generates a
  /// <code>job_id</code>.
  public var reference: JobReference? = nil

  /// Required. Job information, including how, when, and where to
  /// run the job.
  public var placement: JobPlacement? = nil

  /// Output only. The job status. Additional application-specific
  /// status information might be contained in the <code>type_job</code>
  /// and <code>yarn_applications</code> fields.
  public var status: JobStatus? = nil

  /// Output only. The previous job status.
  public var statusHistory: [JobStatus] = []

  /// Output only. The collection of YARN applications spun up by this job.
  ///
  /// **Beta** Feature: This report is available for testing purposes only. It
  /// might be changed before final release.
  public var yarnApplications: [YarnApplication] = []

  /// Output only. A URI pointing to the location of the stdout of the job's
  /// driver program.
  public var driverOutputResourceUri: Swift.String = Swift.String()

  /// Output only. If present, the location of miscellaneous control files
  /// which can be used as part of job setup and handling. If not present,
  /// control files might be placed in the same location as `driver_output_uri`.
  public var driverControlFilesUri: Swift.String = Swift.String()

  /// Optional. The labels to associate with this job.
  /// Label **keys** must contain 1 to 63 characters, and must conform to
  /// [RFC 1035](https://www.ietf.org/rfc/rfc1035.txt).
  /// Label **values** can be empty, but, if present, must contain 1 to 63
  /// characters, and must conform to [RFC
  /// 1035](https://www.ietf.org/rfc/rfc1035.txt). No more than 32 labels can be
  /// associated with a job.
  public var labels: [Swift.String: Swift.String] = [:]

  /// Optional. Job scheduling configuration.
  public var scheduling: JobScheduling? = nil

  /// Output only. A UUID that uniquely identifies a job within the project
  /// over time. This is in contrast to a user-settable reference.job_id that
  /// might be reused over time.
  public var jobUuid: Swift.String = Swift.String()

  /// Output only. Indicates whether the job is completed. If the value is
  /// `false`, the job is still in progress. If `true`, the job is completed, and
  /// `status.state` field will indicate if it was successful, failed,
  /// or cancelled.
  public var done: Swift.Bool = Swift.Bool()

  /// Optional. Driver scheduling configuration.
  public var driverSchedulingConfig: DriverSchedulingConfig? = nil

  /// Required. The application/framework-specific portion of the job.
  public var typeJob: OneOf_TypeJob? = nil

  /// Initialize a new instance of `Job`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Job().with { $0.reference = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case reference = "reference"
    case placement = "placement"
    case hadoopJob = "hadoopJob"
    case sparkJob = "sparkJob"
    case pysparkJob = "pysparkJob"
    case hiveJob = "hiveJob"
    case pigJob = "pigJob"
    case sparkRJob = "sparkRJob"
    case sparkSqlJob = "sparkSqlJob"
    case prestoJob = "prestoJob"
    case trinoJob = "trinoJob"
    case flinkJob = "flinkJob"
    case status = "status"
    case statusHistory = "statusHistory"
    case yarnApplications = "yarnApplications"
    case driverOutputResourceUri = "driverOutputResourceUri"
    case driverControlFilesUri = "driverControlFilesUri"
    case labels = "labels"
    case scheduling = "scheduling"
    case jobUuid = "jobUuid"
    case done = "done"
    case driverSchedulingConfig = "driverSchedulingConfig"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.reference = try container.decodeIfPresent(JobReference.self, forKey: .reference)
    self.placement = try container.decodeIfPresent(JobPlacement.self, forKey: .placement)
    self.status = try container.decodeIfPresent(JobStatus.self, forKey: .status)
    self.statusHistory = try container.decode([JobStatus].self, forKey: .statusHistory)
    self.yarnApplications = try container.decode([YarnApplication].self, forKey: .yarnApplications)
    self.driverOutputResourceUri = try container.decode(
      Swift.String.self, forKey: .driverOutputResourceUri)
    self.driverControlFilesUri = try container.decode(
      Swift.String.self, forKey: .driverControlFilesUri)
    self.labels = try container.decode([Swift.String: Swift.String].self, forKey: .labels)
    self.scheduling = try container.decodeIfPresent(JobScheduling.self, forKey: .scheduling)
    self.jobUuid = try container.decode(Swift.String.self, forKey: .jobUuid)
    self.done = try container.decode(Swift.Bool.self, forKey: .done)
    self.driverSchedulingConfig = try container.decodeIfPresent(
      DriverSchedulingConfig.self, forKey: .driverSchedulingConfig)

    var typeJob: OneOf_TypeJob? = nil
    let typeJobCheckAndSet = {
      if typeJob != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'typeJob'"))
      }
      typeJob = $0
    }
    if let hadoopJob = try container.decodeIfPresent(HadoopJob?.self, forKey: .hadoopJob) {
      try typeJobCheckAndSet(.hadoopJob(hadoopJob))
    }
    if let sparkJob = try container.decodeIfPresent(SparkJob?.self, forKey: .sparkJob) {
      try typeJobCheckAndSet(.sparkJob(sparkJob))
    }
    if let pysparkJob = try container.decodeIfPresent(PySparkJob?.self, forKey: .pysparkJob) {
      try typeJobCheckAndSet(.pysparkJob(pysparkJob))
    }
    if let hiveJob = try container.decodeIfPresent(HiveJob?.self, forKey: .hiveJob) {
      try typeJobCheckAndSet(.hiveJob(hiveJob))
    }
    if let pigJob = try container.decodeIfPresent(PigJob?.self, forKey: .pigJob) {
      try typeJobCheckAndSet(.pigJob(pigJob))
    }
    if let sparkRJob = try container.decodeIfPresent(SparkRJob?.self, forKey: .sparkRJob) {
      try typeJobCheckAndSet(.sparkRJob(sparkRJob))
    }
    if let sparkSqlJob = try container.decodeIfPresent(SparkSqlJob?.self, forKey: .sparkSqlJob) {
      try typeJobCheckAndSet(.sparkSqlJob(sparkSqlJob))
    }
    if let prestoJob = try container.decodeIfPresent(PrestoJob?.self, forKey: .prestoJob) {
      try typeJobCheckAndSet(.prestoJob(prestoJob))
    }
    if let trinoJob = try container.decodeIfPresent(TrinoJob?.self, forKey: .trinoJob) {
      try typeJobCheckAndSet(.trinoJob(trinoJob))
    }
    if let flinkJob = try container.decodeIfPresent(FlinkJob?.self, forKey: .flinkJob) {
      try typeJobCheckAndSet(.flinkJob(flinkJob))
    }
    self.typeJob = typeJob
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.reference, forKey: .reference)
    try container.encode(self.placement, forKey: .placement)
    try container.encode(self.status, forKey: .status)
    try container.encode(self.statusHistory, forKey: .statusHistory)
    try container.encode(self.yarnApplications, forKey: .yarnApplications)
    try container.encode(self.driverOutputResourceUri, forKey: .driverOutputResourceUri)
    try container.encode(self.driverControlFilesUri, forKey: .driverControlFilesUri)
    try container.encode(self.labels, forKey: .labels)
    try container.encode(self.scheduling, forKey: .scheduling)
    try container.encode(self.jobUuid, forKey: .jobUuid)
    try container.encode(self.done, forKey: .done)
    try container.encode(self.driverSchedulingConfig, forKey: .driverSchedulingConfig)

    if let choice = self.typeJob {
      switch choice {
      case .hadoopJob(let value):
        try container.encode(value, forKey: .hadoopJob)
      case .sparkJob(let value):
        try container.encode(value, forKey: .sparkJob)
      case .pysparkJob(let value):
        try container.encode(value, forKey: .pysparkJob)
      case .hiveJob(let value):
        try container.encode(value, forKey: .hiveJob)
      case .pigJob(let value):
        try container.encode(value, forKey: .pigJob)
      case .sparkRJob(let value):
        try container.encode(value, forKey: .sparkRJob)
      case .sparkSqlJob(let value):
        try container.encode(value, forKey: .sparkSqlJob)
      case .prestoJob(let value):
        try container.encode(value, forKey: .prestoJob)
      case .trinoJob(let value):
        try container.encode(value, forKey: .trinoJob)
      case .flinkJob(let value):
        try container.encode(value, forKey: .flinkJob)
      }
    }
  }

  /// Required. The application/framework-specific portion of the job.
  public enum OneOf_TypeJob: Codable, Equatable, Sendable {
    /// Optional. Job is a Hadoop job.
    indirect case hadoopJob(HadoopJob?)
    /// Optional. Job is a Spark job.
    indirect case sparkJob(SparkJob?)
    /// Optional. Job is a PySpark job.
    indirect case pysparkJob(PySparkJob?)
    /// Optional. Job is a Hive job.
    indirect case hiveJob(HiveJob?)
    /// Optional. Job is a Pig job.
    indirect case pigJob(PigJob?)
    /// Optional. Job is a SparkR job.
    indirect case sparkRJob(SparkRJob?)
    /// Optional. Job is a SparkSql job.
    indirect case sparkSqlJob(SparkSqlJob?)
    /// Optional. Job is a Presto job.
    indirect case prestoJob(PrestoJob?)
    /// Optional. Job is a Trino job.
    indirect case trinoJob(TrinoJob?)
    /// Optional. Job is a Flink job.
    indirect case flinkJob(FlinkJob?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.dataproc.v1.Job"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
