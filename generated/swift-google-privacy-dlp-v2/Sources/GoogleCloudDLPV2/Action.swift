// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// A task to execute on the completion of a job.
/// See
/// https://docs.cloud.google.com/sensitive-data-protection/docs/concepts-actions
/// to learn more.
public struct Action: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// Extra events to execute after the job has finished.
  public var action: OneOf_Action? = nil

  /// Initialize a new instance of `Action`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Action().with { $0.saveFindings = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case saveFindings = "saveFindings"
    case pubSub = "pubSub"
    case publishSummaryToCscc = "publishSummaryToCscc"
    case publishFindingsToCloudDataCatalog = "publishFindingsToCloudDataCatalog"
    case publishFindingsToDataplexCatalog = "publishFindingsToDataplexCatalog"
    case deidentify = "deidentify"
    case jobNotificationEmails = "jobNotificationEmails"
    case publishToStackdriver = "publishToStackdriver"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)

    var action: OneOf_Action? = nil
    let actionCheckAndSet = {
      if action != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'action'"))
      }
      action = $0
    }
    if let saveFindings = try container.decodeIfPresent(
      Action.SaveFindings?.self, forKey: .saveFindings)
    {
      try actionCheckAndSet(.saveFindings(saveFindings))
    }
    if let pubSub = try container.decodeIfPresent(Action.PublishToPubSub?.self, forKey: .pubSub) {
      try actionCheckAndSet(.pubSub(pubSub))
    }
    if let publishSummaryToCscc = try container.decodeIfPresent(
      Action.PublishSummaryToCscc?.self, forKey: .publishSummaryToCscc)
    {
      try actionCheckAndSet(.publishSummaryToCscc(publishSummaryToCscc))
    }
    if let publishFindingsToCloudDataCatalog = try container.decodeIfPresent(
      Action.PublishFindingsToCloudDataCatalog?.self, forKey: .publishFindingsToCloudDataCatalog)
    {
      try actionCheckAndSet(.publishFindingsToCloudDataCatalog(publishFindingsToCloudDataCatalog))
    }
    if let publishFindingsToDataplexCatalog = try container.decodeIfPresent(
      Action.PublishFindingsToDataplexCatalog?.self, forKey: .publishFindingsToDataplexCatalog)
    {
      try actionCheckAndSet(.publishFindingsToDataplexCatalog(publishFindingsToDataplexCatalog))
    }
    if let deidentify = try container.decodeIfPresent(Action.Deidentify?.self, forKey: .deidentify)
    {
      try actionCheckAndSet(.deidentify(deidentify))
    }
    if let jobNotificationEmails = try container.decodeIfPresent(
      Action.JobNotificationEmails?.self, forKey: .jobNotificationEmails)
    {
      try actionCheckAndSet(.jobNotificationEmails(jobNotificationEmails))
    }
    if let publishToStackdriver = try container.decodeIfPresent(
      Action.PublishToStackdriver?.self, forKey: .publishToStackdriver)
    {
      try actionCheckAndSet(.publishToStackdriver(publishToStackdriver))
    }
    self.action = action
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)

    if let choice = self.action {
      switch choice {
      case .saveFindings(let value):
        try container.encode(value, forKey: .saveFindings)
      case .pubSub(let value):
        try container.encode(value, forKey: .pubSub)
      case .publishSummaryToCscc(let value):
        try container.encode(value, forKey: .publishSummaryToCscc)
      case .publishFindingsToCloudDataCatalog(let value):
        try container.encode(value, forKey: .publishFindingsToCloudDataCatalog)
      case .publishFindingsToDataplexCatalog(let value):
        try container.encode(value, forKey: .publishFindingsToDataplexCatalog)
      case .deidentify(let value):
        try container.encode(value, forKey: .deidentify)
      case .jobNotificationEmails(let value):
        try container.encode(value, forKey: .jobNotificationEmails)
      case .publishToStackdriver(let value):
        try container.encode(value, forKey: .publishToStackdriver)
      }
    }
  }

  /// If set, the detailed findings will be persisted to the specified
  /// OutputStorageConfig. Only a single instance of this action can be
  /// specified.
  /// Compatible with: Inspect, Risk
  public struct SaveFindings: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Location to store findings outside of DLP.
    public var outputConfig: OutputStorageConfig? = nil

    /// Initialize a new instance of `SaveFindings`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = SaveFindings().with { $0.outputConfig = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.privacy.dlp.v2.Action.SaveFindings"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Publish a message into a given Pub/Sub topic when DlpJob has completed. The
  /// message contains a single field, `DlpJobName`, which is equal to the
  /// finished job's
  /// [`DlpJob.name`](https://docs.cloud.google.com/sensitive-data-protection/docs/reference/rest/v2/projects.dlpJobs#DlpJob).
  /// Compatible with: Inspect, Risk
  public struct PublishToPubSub: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Cloud Pub/Sub topic to send notifications to. The topic must have given
    /// publishing access rights to the DLP API service account executing
    /// the long running DlpJob sending the notifications.
    /// Format is projects/{project}/topics/{topic}.
    public var topic: Swift.String = Swift.String()

    /// Initialize a new instance of `PublishToPubSub`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = PublishToPubSub().with { $0.topic = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.privacy.dlp.v2.Action.PublishToPubSub"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Publish the result summary of a DlpJob to [Security Command
  /// Center](https://cloud.google.com/security-command-center). This action is
  /// available for only projects that belong to an organization. This action
  /// publishes the count of finding instances and their infoTypes. The summary
  /// of findings are persisted in Security Command Center and are governed by
  /// [service-specific policies for Security Command
  /// Center](https://cloud.google.com/terms/service-terms). Only a single
  /// instance of this action can be specified. Compatible with: Inspect
  public struct PublishSummaryToCscc: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `PublishSummaryToCscc`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = PublishSummaryToCscc().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.privacy.dlp.v2.Action.PublishSummaryToCscc"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Publish findings of a DlpJob to Data Catalog. In Data Catalog, tag
  /// templates are applied to the resource that Cloud DLP scanned. Data
  /// Catalog tag templates are stored in the same project and region where the
  /// BigQuery table exists. For Cloud DLP to create and apply the tag template,
  /// the Cloud DLP service agent must have the
  /// `roles/datacatalog.tagTemplateOwner` permission on the project. The tag
  /// template contains fields summarizing the results of the DlpJob. Any field
  /// values previously written by another DlpJob are deleted. [InfoType naming
  /// patterns][google.privacy.dlp.v2.InfoType] are strictly enforced when using
  /// this feature.
  ///
  /// Findings are persisted in Data Catalog storage and are governed by
  /// service-specific policies for Data Catalog. For more information, see
  /// [Service Specific Terms](https://cloud.google.com/terms/service-terms).
  ///
  /// Only a single instance of this action can be specified. This action is
  /// allowed only if all resources being scanned are BigQuery tables.
  /// Compatible with: Inspect
  ///
  /// [google.privacy.dlp.v2.InfoType]: <doc:InfoType>
  public struct PublishFindingsToCloudDataCatalog: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `PublishFindingsToCloudDataCatalog`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = PublishFindingsToCloudDataCatalog().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.privacy.dlp.v2.Action.PublishFindingsToCloudDataCatalog"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Publish findings of a DlpJob to Dataplex Universal Catalog as a
  /// `sensitive-data-protection-job-result` aspect. For more information,
  /// see [Send inspection results to Dataplex Universal Catalog as
  /// aspects](https://docs.cloud.google.com/sensitive-data-protection/docs/add-aspects-inspection-job).
  ///
  /// Aspects are stored in Dataplex Universal Catalog storage and are
  /// governed by service-specific policies for Dataplex Universal Catalog. For
  /// more information, see [Service Specific
  /// Terms](https://cloud.google.com/terms/service-terms).
  ///
  /// Only a single instance of this action can be specified. This action is
  /// allowed only if all resources being scanned are BigQuery tables.
  /// Compatible with: Inspect
  public struct PublishFindingsToDataplexCatalog: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `PublishFindingsToDataplexCatalog`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = PublishFindingsToDataplexCatalog().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.privacy.dlp.v2.Action.PublishFindingsToDataplexCatalog"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Create a de-identified copy of a storage bucket. Only compatible
  /// with Cloud Storage buckets.
  ///
  ///
  /// A TransformationDetail will be created for each transformation.
  ///
  ///
  /// Compatible with: Inspection of Cloud Storage
  public struct Deidentify: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// User specified deidentify templates and configs for structured,
    /// unstructured, and image files.
    public var transformationConfig: TransformationConfig? = nil

    /// Config for storing transformation details.
    ///
    /// This field specifies the configuration for storing detailed metadata
    /// about each transformation performed during a de-identification process.
    /// The metadata is stored separately from the de-identified content itself
    /// and provides a granular record of both successful transformations and any
    /// failures that occurred.
    ///
    /// Enabling this configuration is essential for users who need to access
    /// comprehensive information about the status, outcome, and specifics of
    /// each transformation. The details are captured in the
    /// [TransformationDetails][google.privacy.dlp.v2.TransformationDetails]
    /// message for each operation.
    ///
    /// Key use cases:
    ///
    /// * **Auditing and compliance**
    ///     * Provides a verifiable audit trail of de-identification activities,
    ///     which is crucial for meeting regulatory requirements and internal
    ///     data governance policies.
    ///     * Logs what data was transformed, what transformations were applied,
    ///     when they occurred, and their success status. This helps
    ///     demonstrate accountability and due diligence in protecting
    ///     sensitive data.
    ///
    /// * **Troubleshooting and debugging**
    ///     * Offers detailed error messages and context if a transformation
    ///     fails. This information is useful for diagnosing and resolving
    ///     issues in the de-identification pipeline.
    ///     * Helps pinpoint the exact location and nature of failures, speeding
    ///     up the debugging process.
    ///
    /// * **Process verification and quality assurance**
    ///     * Allows users to confirm that de-identification rules and
    ///     transformations were applied correctly and consistently across
    ///     the dataset as intended.
    ///     * Helps in verifying the effectiveness of the chosen
    ///     de-identification strategies.
    ///
    /// * **Data lineage and impact analysis**
    ///     * Creates a record of how data elements were modified, contributing
    ///     to data lineage. This is useful for understanding the provenance
    ///     of de-identified data.
    ///     * Aids in assessing the potential impact of de-identification choices
    ///     on downstream analytical processes or data usability.
    ///
    /// * **Reporting and operational insights**
    ///     * You can analyze the metadata stored in a queryable BigQuery table
    ///     to generate reports on transformation success rates, common
    ///     error types, processing volumes (e.g., transformedBytes), and the
    ///     types of transformations applied.
    ///     * These insights can inform optimization of de-identification
    ///     configurations and resource planning.
    ///
    /// To take advantage of these benefits, set this configuration. The stored
    /// details include a description of the transformation, success or
    /// error codes, error messages, the number of bytes transformed, the
    /// location of the transformed content, and identifiers for the job and
    /// source data.
    ///
    /// [google.privacy.dlp.v2.TransformationDetails]: <doc:TransformationDetails>
    public var transformationDetailsStorageConfig: TransformationDetailsStorageConfig? = nil

    /// List of user-specified file type groups to transform. If specified, only
    /// the files with these file types are transformed. If empty, all
    /// supported files are transformed. Supported types may be automatically
    /// added over time. Any unsupported file types that are set in this field
    /// are excluded from de-identification. An error is recorded for each
    /// unsupported file in the TransformationDetails output table. Currently the
    /// only file types supported are: IMAGES, TEXT_FILES, CSV, TSV.
    public var fileTypesToTransform: [FileType] = []

    /// Where to store the output.
    public var output: OneOf_Output? = nil

    /// Initialize a new instance of `Deidentify`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Deidentify().with { $0.transformationConfig = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case transformationConfig = "transformationConfig"
      case transformationDetailsStorageConfig = "transformationDetailsStorageConfig"
      case cloudStorageOutput = "cloudStorageOutput"
      case fileTypesToTransform = "fileTypesToTransform"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.transformationConfig = try container.decodeIfPresent(
        TransformationConfig.self, forKey: .transformationConfig)
      self.transformationDetailsStorageConfig = try container.decodeIfPresent(
        TransformationDetailsStorageConfig.self, forKey: .transformationDetailsStorageConfig)
      self.fileTypesToTransform = try container.decode(
        [FileType].self, forKey: .fileTypesToTransform)

      var output: OneOf_Output? = nil
      let outputCheckAndSet = {
        if output != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'output'"))
        }
        output = $0
      }
      if let cloudStorageOutput = try container.decodeIfPresent(
        Swift.String.self, forKey: .cloudStorageOutput)
      {
        try outputCheckAndSet(.cloudStorageOutput(cloudStorageOutput))
      }
      self.output = output
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.transformationConfig, forKey: .transformationConfig)
      try container.encode(
        self.transformationDetailsStorageConfig, forKey: .transformationDetailsStorageConfig)
      try container.encode(self.fileTypesToTransform, forKey: .fileTypesToTransform)

      if let choice = self.output {
        switch choice {
        case .cloudStorageOutput(let value):
          try container.encode(value, forKey: .cloudStorageOutput)
        }
      }
    }

    /// Where to store the output.
    public enum OneOf_Output: Codable, Equatable, Sendable {
      /// Required. User settable Cloud Storage bucket and folders to store
      /// de-identified files. This field must be set for Cloud Storage
      /// deidentification. The output Cloud Storage bucket must be different
      /// from the input bucket. De-identified files will overwrite files in the
      /// output path.
      ///
      /// Form of: gs://bucket/folder/ or gs://bucket
      case cloudStorageOutput(Swift.String)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.privacy.dlp.v2.Action.Deidentify"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Sends an email when the job completes. The email goes to IAM project owners
  /// and technical [Essential
  /// Contacts](https://docs.cloud.google.com/resource-manager/docs/managing-notification-contacts).
  public struct JobNotificationEmails: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `JobNotificationEmails`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = JobNotificationEmails().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.privacy.dlp.v2.Action.JobNotificationEmails"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Enable Stackdriver metric dlp.googleapis.com/finding_count. This
  /// will publish a metric to stack driver on each infotype requested and
  /// how many findings were found for it. CustomDetectors will be bucketed
  /// as 'Custom' under the Stackdriver label 'info_type'.
  public struct PublishToStackdriver: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Initialize a new instance of `PublishToStackdriver`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = PublishToStackdriver().with { $0.<placeholder> = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.privacy.dlp.v2.Action.PublishToStackdriver"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Extra events to execute after the job has finished.
  public enum OneOf_Action: Codable, Equatable, Sendable {
    /// Save resulting findings in a provided location.
    indirect case saveFindings(Action.SaveFindings?)
    /// Publish a notification to a Pub/Sub topic.
    indirect case pubSub(Action.PublishToPubSub?)
    /// Publish summary to Cloud Security Command Center (Alpha).
    indirect case publishSummaryToCscc(Action.PublishSummaryToCscc?)
    /// Deprecated because Data Catalog is being turned down. Use
    /// publish_findings_to_dataplex_catalog to publish findings to Dataplex
    /// Universal Catalog.
    @available(*, deprecated)
    indirect case publishFindingsToCloudDataCatalog(Action.PublishFindingsToCloudDataCatalog?)
    /// Publish findings as an aspect to Dataplex Universal Catalog.
    indirect case publishFindingsToDataplexCatalog(Action.PublishFindingsToDataplexCatalog?)
    /// Create a de-identified copy of the input data.
    indirect case deidentify(Action.Deidentify?)
    /// Sends an email when the job completes. The email goes to IAM project
    /// owners and technical [Essential
    /// Contacts](https://docs.cloud.google.com/resource-manager/docs/managing-notification-contacts).
    indirect case jobNotificationEmails(Action.JobNotificationEmails?)
    /// Enable Stackdriver metric dlp.googleapis.com/finding_count.
    indirect case publishToStackdriver(Action.PublishToStackdriver?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.privacy.dlp.v2.Action"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
