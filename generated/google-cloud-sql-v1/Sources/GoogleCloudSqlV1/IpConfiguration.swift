// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#if SqlBackupsService || SqlInstancesService
  import Foundation
  @_spi(GoogleCloudInternal) import GoogleCloudWkt

  /// IP Management configuration.
  public struct IpConfiguration: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Whether the instance is assigned a public IP address or not.
    public var ipv4Enabled: GoogleCloudWkt.BoolValue? = nil

    /// The resource link for the VPC network from which the Cloud SQL instance is
    /// accessible for private IP. For example,
    /// `/projects/myProject/global/networks/default`. This setting can
    /// be updated, but it cannot be removed after it is set.
    public var privateNetwork: Swift.String = Swift.String()

    /// Use `ssl_mode` instead.
    ///
    /// Whether SSL/TLS connections over IP are enforced.
    /// If set to false, then allow both non-SSL/non-TLS and SSL/TLS connections.
    /// For SSL/TLS connections, the client certificate won't be verified. If
    /// set to true, then only allow connections encrypted with SSL/TLS and with
    /// valid client certificates. If you want to enforce SSL/TLS without enforcing
    /// the requirement for valid client certificates, then use the `ssl_mode` flag
    /// instead of the `require_ssl` flag.
    public var requireSsl: GoogleCloudWkt.BoolValue? = nil

    /// The list of external networks that are allowed to connect to the instance
    /// using the IP. In 'CIDR' notation, also known as 'slash' notation (for
    /// example: `157.197.200.0/24`).
    public var authorizedNetworks: [AclEntry] = []

    /// The name of the allocated ip range for the private ip Cloud SQL instance.
    /// For example: "google-managed-services-default". If set, the instance ip
    /// will be created in the allocated range. The range name must comply with
    /// [RFC 1035](https://tools.ietf.org/html/rfc1035). Specifically, the name
    /// must be 1-63 characters long and match the regular expression
    /// `[a-z]([-a-z0-9]*[a-z0-9])?.`
    public var allocatedIpRange: Swift.String = Swift.String()

    /// Controls connectivity to private IP instances from Google services,
    /// such as BigQuery.
    public var enablePrivatePathForGoogleCloudServices: GoogleCloudWkt.BoolValue? = nil

    /// Specify how SSL/TLS is enforced in database connections. If you must use
    /// the `require_ssl` flag for backward compatibility, then only the following
    /// value pairs are valid:
    ///
    /// For PostgreSQL and MySQL:
    ///
    /// * `ssl_mode=ALLOW_UNENCRYPTED_AND_ENCRYPTED` and `require_ssl=false`
    /// * `ssl_mode=ENCRYPTED_ONLY` and `require_ssl=false`
    /// * `ssl_mode=TRUSTED_CLIENT_CERTIFICATE_REQUIRED` and `require_ssl=true`
    ///
    /// For SQL Server:
    ///
    /// * `ssl_mode=ALLOW_UNENCRYPTED_AND_ENCRYPTED` and `require_ssl=false`
    /// * `ssl_mode=ENCRYPTED_ONLY` and `require_ssl=true`
    ///
    /// The value of `ssl_mode` has priority over the value of `require_ssl`.
    ///
    /// For example, for the pair `ssl_mode=ENCRYPTED_ONLY` and
    /// `require_ssl=false`, `ssl_mode=ENCRYPTED_ONLY` means accept only SSL
    /// connections, while `require_ssl=false` means accept both non-SSL
    /// and SSL connections. In this case, MySQL and PostgreSQL databases respect
    /// `ssl_mode` and accepts only SSL connections.
    public var sslMode: IpConfiguration.SslMode = IpConfiguration.SslMode()

    /// PSC settings for this instance.
    public var pscConfig: PscConfig? = nil

    /// Specify what type of CA is used for the server certificate.
    public var serverCaMode: IpConfiguration.CaMode? = nil

    /// Optional. Custom Subject Alternative Name(SAN)s for a Cloud SQL instance.
    public var customSubjectAlternativeNames: [Swift.String] = []

    /// Optional. The resource name of the server CA pool for an instance with
    /// `CUSTOMER_MANAGED_CAS_CA` as the `server_ca_mode`.
    /// Format: projects/{PROJECT}/locations/{REGION}/caPools/{CA_POOL_ID}
    public var serverCaPool: Swift.String? = nil

    /// Optional. Controls the automatic server certificate rotation feature. This
    /// feature is disabled by default. When enabled, the server certificate will
    /// be automatically rotated during Cloud SQL scheduled maintenance or
    /// self-service maintenance updates up to six months before it expires. This
    /// setting can only be set if server_ca_mode is either GOOGLE_MANAGED_CAS_CA
    /// or CUSTOMER_MANAGED_CAS_CA.
    public var serverCertificateRotationMode: IpConfiguration.ServerCertificateRotationMode? = nil

    /// Initialize a new instance of `IpConfiguration`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = IpConfiguration().with { $0.ipv4Enabled = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// The SSL options for database connections.
    public enum SslMode: Codable, Equatable, Sendable {
      /// The SSL mode is unknown.
      case unspecified
      /// Allow non-SSL/non-TLS and SSL/TLS connections.
      /// For SSL connections to MySQL and PostgreSQL, the client certificate
      /// isn't verified.
      ///
      /// When this value is used, the legacy `require_ssl` flag must be false or
      /// cleared to avoid a conflict between the values of the two flags.
      case allowUnencryptedAndEncrypted
      /// Only allow connections encrypted with SSL/TLS.
      /// For SSL connections to MySQL and PostgreSQL, the client certificate
      /// isn't verified.
      ///
      /// When this value is used, the legacy `require_ssl` flag must be false or
      /// cleared to avoid a conflict between the values of the two flags.
      case encryptedOnly
      /// Only allow connections encrypted with SSL/TLS and with valid
      /// client certificates.
      ///
      /// When this value is used, the legacy `require_ssl` flag must be true or
      /// cleared to avoid the conflict between values of two flags.
      /// PostgreSQL clients or users that connect using IAM database
      /// authentication must use either the
      /// [Cloud SQL Auth
      /// Proxy](https://cloud.google.com/sql/docs/postgres/connect-auth-proxy) or
      /// [Cloud SQL
      /// Connectors](https://cloud.google.com/sql/docs/postgres/connect-connectors)
      /// to enforce client identity verification.
      ///
      /// Only applicable to MySQL and PostgreSQL. Not applicable to SQL Server.
      case trustedClientCertificateRequired
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .allowUnencryptedAndEncrypted: return 1
        case .encryptedOnly: return 2
        case .trustedClientCertificateRequired: return 3
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "SSL_MODE_UNSPECIFIED"
        case .allowUnencryptedAndEncrypted: return "ALLOW_UNENCRYPTED_AND_ENCRYPTED"
        case .encryptedOnly: return "ENCRYPTED_ONLY"
        case .trustedClientCertificateRequired: return "TRUSTED_CLIENT_CERTIFICATE_REQUIRED"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:SslMode/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "SSL_MODE_UNSPECIFIED": self = .unspecified
        case "ALLOW_UNENCRYPTED_AND_ENCRYPTED": self = .allowUnencryptedAndEncrypted
        case "ENCRYPTED_ONLY": self = .encryptedOnly
        case "TRUSTED_CLIENT_CERTIFICATE_REQUIRED": self = .trustedClientCertificateRequired
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:SslMode/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .allowUnencryptedAndEncrypted
        case 2: self = .encryptedOnly
        case 3: self = .trustedClientCertificateRequired
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .allowUnencryptedAndEncrypted: return try container.encode(1)
        case .encryptedOnly: return try container.encode(2)
        case .trustedClientCertificateRequired: return try container.encode(3)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// Various Certificate Authority (CA) modes for certificate signing.
    public enum CaMode: Codable, Equatable, Sendable {
      /// CA mode is unspecified. It is effectively the same as
      /// `GOOGLE_MANAGED_INTERNAL_CA`.
      case unspecified
      /// Google-managed self-signed internal CA.
      case googleManagedInternalCa
      /// Google-managed regional CA part of root CA hierarchy hosted on Google
      /// Cloud's Certificate Authority Service (CAS).
      case googleManagedCasCa
      /// Customer-managed CA hosted on Google Cloud's Certificate Authority
      /// Service (CAS).
      case customerManagedCasCa
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .googleManagedInternalCa: return 1
        case .googleManagedCasCa: return 2
        case .customerManagedCasCa: return 3
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "CA_MODE_UNSPECIFIED"
        case .googleManagedInternalCa: return "GOOGLE_MANAGED_INTERNAL_CA"
        case .googleManagedCasCa: return "GOOGLE_MANAGED_CAS_CA"
        case .customerManagedCasCa: return "CUSTOMER_MANAGED_CAS_CA"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:CaMode/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "CA_MODE_UNSPECIFIED": self = .unspecified
        case "GOOGLE_MANAGED_INTERNAL_CA": self = .googleManagedInternalCa
        case "GOOGLE_MANAGED_CAS_CA": self = .googleManagedCasCa
        case "CUSTOMER_MANAGED_CAS_CA": self = .customerManagedCasCa
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:CaMode/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .googleManagedInternalCa
        case 2: self = .googleManagedCasCa
        case 3: self = .customerManagedCasCa
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .googleManagedInternalCa: return try container.encode(1)
        case .googleManagedCasCa: return try container.encode(2)
        case .customerManagedCasCa: return try container.encode(3)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// Settings for automatic server certificate rotation.
    public enum ServerCertificateRotationMode: Codable, Equatable, Sendable {
      /// Unspecified: no automatic server certificate rotation.
      case unspecified
      /// No automatic server certificate rotation. The user must [manage server
      /// certificate
      /// rotation](/sql/docs/mysql/manage-ssl-instance#rotate-server-certificate-cas)
      /// on their side.
      case noAutomaticRotation
      /// Automatic server certificate rotation during Cloud SQL scheduled
      /// maintenance or self-service maintenance updates. Requires
      /// `server_ca_mode` to be `GOOGLE_MANAGED_CAS_CA` or
      /// `CUSTOMER_MANAGED_CAS_CA`.
      case automaticRotationDuringMaintenance
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .noAutomaticRotation: return 1
        case .automaticRotationDuringMaintenance: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "SERVER_CERTIFICATE_ROTATION_MODE_UNSPECIFIED"
        case .noAutomaticRotation: return "NO_AUTOMATIC_ROTATION"
        case .automaticRotationDuringMaintenance: return "AUTOMATIC_ROTATION_DURING_MAINTENANCE"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ServerCertificateRotationMode/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "SERVER_CERTIFICATE_ROTATION_MODE_UNSPECIFIED": self = .unspecified
        case "NO_AUTOMATIC_ROTATION": self = .noAutomaticRotation
        case "AUTOMATIC_ROTATION_DURING_MAINTENANCE": self = .automaticRotationDuringMaintenance
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ServerCertificateRotationMode/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .noAutomaticRotation
        case 2: self = .automaticRotationDuringMaintenance
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .noAutomaticRotation: return try container.encode(1)
        case .automaticRotationDuringMaintenance: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.sql.v1.IpConfiguration"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }
#endif
